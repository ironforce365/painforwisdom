bulk-ingestion-up-to-feb-27/
processed/
.DS_Store*
.env
.env.sandbox
.env.sandbox.swp

# Input files — never commit transcripts or videos
to_be_retried/*
bulk-*

# LangGraph migration — runtime/sandbox artifacts
obsidian-vault-sandbox/
pipeline/checkpoints*.db
poc/runs.jsonl
.current_run
.paperclip.json
.claude/scheduled_tasks.lock

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Daily summarizer runtime artifacts (Phase 2-6 of look-at-my-notion-serialized-donut)
# books/ = raw + extracted book text from z-library + curated copies
# briefs/ = generated daily brief markdown bundles
# reports/ = audit + augment + run telemetry
# pipeline/state/ = theme_stats.json + per-theme dedupe caches
books/
briefs/
bulk/
reports/
pipeline/state/
pipeline/checkpoints.db-wal
pipeline/checkpoints.db-shm

# Tooling scratch + runtime state
.playwright-mcp/
*.log
data/.daily_brief_watchdog.last_alert

# Local draft markdown (homepage candidates, blog post drafts).
# Final versions live elsewhere; keep the repo root + tracked dirs clean.
data/temp/

# Coach service
services/coach/.env
eval-runs/
