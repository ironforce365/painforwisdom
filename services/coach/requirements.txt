# Dependency manifest for the Docker build's heavy layer.
#
# This MIRRORS pyproject.toml [project.dependencies] + [project.optional-
# dependencies].test. It exists so the Dockerfile can key the heavy (torch)
# install layer on the dependency list ALONE — a version-only release bumps
# pyproject.toml but not this file, so the layer stays cached and torch is not
# reinstalled. pyproject.toml remains the source of truth for deps.
#
# REGENERATE THIS when you add/remove/bump a dependency in pyproject.toml.
# tests/test_requirements_sync.py fails if this file drifts from pyproject.
#
# --- runtime (pyproject [project.dependencies]) ---
claude-agent-sdk>=0.2.87
python-telegram-bot[ext]>=22.0
faster-whisper>=1.0.3
llama-index-core>=0.14.6
llama-index-readers-obsidian>=0.7.1
llama-index-embeddings-openai>=0.6.0
llama-index-llms-anthropic>=0.10.0
llama-index-retrievers-bm25>=0.6.0
sentence-transformers>=3.0.0
mcp>=1.4.0
fastmcp>=2.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pyyaml>=6.0
notion-client>=2.2.0
openai>=1.50.0
# --- test (pyproject [project.optional-dependencies].test) ---
pytest>=8.0
pytest-asyncio>=0.24
pytest-mock>=3.14
respx>=0.21
